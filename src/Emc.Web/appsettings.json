{
  "ConnectionStrings": {
    "Emc": "Server=.;Database=Emc;Trusted_Connection=True;TrustServerCertificate=True;Encrypt=True"
  },
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning",
      "Microsoft.EntityFrameworkCore.Database.Command": "Warning"
    }
  },
  "AllowedHosts": "*",
  "SourceDocuments": {
    "_comment": "RootPath MUST be outside the site's physical path. The application-pool identity needs Modify on it. The web process never renders (DOC-014): page-count, pixel, DPI and timeout limits here are read by the worker, whose own appsettings.json is authoritative for rendering; the web application uses only RootPath, MaxContentBytes and DuplicateRequestWindowSeconds.",
    "RootPath": "D:\\EmcSourceDocuments",
    "MaxContentBytes": 52428800,
    "MaxPageCount": 200,
    "MaxPixelsPerPage": 30000000,
    "RenderDpi": 150,
    "RenderTimeoutSeconds": 60,
    "DuplicateRequestWindowSeconds": 120
  }
}
